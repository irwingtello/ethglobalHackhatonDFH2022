import { IdToken } from '@uauth/common';
declare const verifyIdToken: (jwks_uri: string | undefined, jwks: string | undefined, id_token: string, nonce: string) => Promise<IdToken>;
export default verifyIdToken;
